---
  - name: Check if directory exists
    stat:
      path: /var/lib/monitoring-prometheus
    register: dir_check

  - name: Clone repository
    git:
        repo: "{{ repo_url }}"
        dest: /var/lib/monitoring-prometheus
    when: not dir_check.stat.exists

  - name: Replace IP address in Prometheus config file
    replace:
        path: /var/lib/monitoring-prometheus/prometheus.yml
        regexp: '(\d{1,3}\.){3}\d{1,3}'
        replace: '{{ public_ip }}'
